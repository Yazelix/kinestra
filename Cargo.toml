[package]
name = "kinestra"
version = "0.2.0"
edition = "2024"
license = "Apache-2.0"
description = "Typed orchestration for recordings of real terminal applications"
autotests = false

[dependencies]
signal-hook = { version = "0.4.4", default-features = false }

[[example]]
name = "capture-check"
path = "tests/capture.rs"
